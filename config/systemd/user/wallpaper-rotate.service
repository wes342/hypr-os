[Unit]
Description=Rotate wallpaper and theme
After=graphical-session.target

[Service]
Type=oneshot
# Only kill the main script on stop, not child processes (wallpaper daemons, waybar)
KillMode=process
Environment=HYPR_OS_DIR=%h/dev/hypr-os
ExecStart=%h/dev/hypr-os/scripts/wallpaper.sh
