# ┌──────────────────────────────────────────┐
# │              Monitor Config              │
# └──────────────────────────────────────────┘
# Primary: LG ULTRAGEAR 32" 1440p 180Hz
# Format: monitor = name, resolution@rate, position, scale

monitor = DP-3, 2560x1440@180, 0x0, 1, vrr, 1

# Sensor panel: ZeroMOD 8" (native 480x1920, rotated to 1920x480)
# Positioned at 0x5000 — non-adjacent to primary so cursor cannot cross.
monitor = HDMI-A-2, 480x1920@60, 0x5000, 1, transform, 3

# Fallback for any other connected monitor
monitor = , preferred, auto, 1
