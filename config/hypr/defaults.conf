# ┌──────────────────────────────────────────┐
# │          Default Applications            │
# │  Change these to update everywhere       │
# └──────────────────────────────────────────┘

$terminal = kitty
$browser = firefox
$fileManager = thunar
$menu = rofi -show drun -show-icons
